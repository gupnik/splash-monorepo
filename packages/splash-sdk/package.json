{
  "name": "@splash/sdk",
  "version": "0.0.1",
  "description": "Useful tooling for interacting with the Splash protocol",
  "author": "Nikhil Gupta",
  "homepage": "",
  "license": "GPL-3.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist tsconfig.build.tsbuildinfo",
    "build": "tsc -p tsconfig.build.json",
    "prepare": "yarn build",
    "test": "mocha -r ts-node/register \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@splash/contracts": "^0.0.1",
    "ethers": "^5.4.7"
  },
  "peerDependencies": {
    "ethers": ">=5.0.0"
  }
}
